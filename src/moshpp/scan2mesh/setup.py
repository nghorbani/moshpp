import setuptools

setuptools.setup(
    name="scan2mesh",
    version="0.0.1",
    description="chumpy package to calculate scan to mesh distance",
    packages=setuptools.find_packages(),
    install_requires=['chumpy']
)
